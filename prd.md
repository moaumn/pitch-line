# 产品需求文档 (PRD) - 小程序「准音线 (PitchLine)」V2.5 终版

## 1. 产品概述与商业价值

### 1.1 产品定义与核心功能
**「准音线 (PitchLine)」** 是一款单机轻量化、免服务器的离线音高纠正与视觉练习小程序。
* **核心功能**：支持用户导入“伴奏轨”与“纯人声干声轨”，通过在手机端离线解码音频、切片，并利用音高算法提取原唱音高曲线，与用户的录音进行双轨 Canvas 2D 波形对比与相似度打分。
* **核心原则**：**0 服务器开销**（完全依赖客户端算力），纯单机安全运行，规避微信高危音视频类目审核。

### 1.2 商业模式
* **商业设计**：核心高价值的“跟读模式”作为 VIP 栅栏受控项，支持未来通过广告解锁或付费激活。

---

## 2. 核心功能需求详述

### 模块一：双轨素材导入与安全存储 (Dual-Track Ingestion)

#### 📋 FR-1.1 双轨导入入口
* **轨道 A（原曲/伴奏轨 - 可选）**：用于播放给用户听，辅助找调。支持格式：`MP4` / `MOV` / `MP3` / `WAV`。
* **轨道 B（原唱人声轨 - 必填）**：隐藏在后台专门用于算法分析。要求必须是已声源分离的**“纯人声/干声”**文件。支持格式：`MP3` / `WAV`。

#### 📋 FR-1.2 弹性文件尺寸与时长策略（测试边界放宽）

| 区域划分 | 条件判定 | 处理逻辑 & UI 反馈 |
| :--- | :--- | :--- |
| **安全区** | 体积 $\le 10\text{MB}$ 且 时长 $\le 3\text{分钟}$ | 正常静默导入。 |
| **边界区** | 超过安全区，但未达最大限制 | 允许导入。UI 需弹出非阻塞式弱提示（Toast）：“*当前文件较大，正在进入边界性能测试，解码时间可能较长，请注意手机发热。*” |
| **熔断区** | 体积 $> 50\text{MB}$ 或 时长 $> 10\text{分钟}$ | 强制拦截并弹窗警告，不允许导入。 |

#### 📋 FR-1.3 本地安全转存
* **处理逻辑**：导入成功后，立即使用 `FileSystemManager.saveFile` 将两个文件的临时路径转存至小程序持久化本地目录 `wx.env.USER_DATA_PATH`，防止系统清理。

---

### 模块二：交互双模式与防回音污染 (Dual Mode Logic & AEC)

用户可在界面自由切换以下两种核心交互模式：

#### 🔄 模式 1：自动模式 (Auto Sync Mode) - 基础免费项
* **交互流程**：用户点击“开始”，系统同时播放轨道 A 并启动录音，用户边听边唱，动态绘制录音线直至手动暂停或歌曲结束。
* **硬件防污染**：由于录音与伴奏播放并存，`wx.getRecorderManager()` 启动参数必须强制配置 `audioSource: 'voice_communication'`，激活手机芯片级回音消除（AEC）与降噪，过滤扬声器出来的伴奏声。
* **UI 指导**：提示文案：“*当前模式下，请务必佩戴耳机练习以获得精准的打分*”。

#### 🔄 模式 2：跟读模式 (Shadowing Mode) - 商业受控项
* **交互流程**：
  * **按住状态 (`bindtouchstart`)**：播放当前片段的轨道 A，同时启动系统时钟，记录片段开始时间点 $T_{\text{start}}$。此时不录音。
  * **松开状态 (`bindtouchend`)**：轨道 A 暂停播放，记录结束时间点 $T_{\text{end}}$。
* **触发分析与绘制**：
  1. 触发权限栅栏校验（详见 **FR-4.4**）。
  2. 校验通过后，系统根据 $[T_{\text{start}}, T_{\text{end}}]$ 时间区间，在 Worker 线程中对轨道 B（原唱人声）进行 PCM 二进制数据切片（`slice()`）。
  3. 提取音高并作为**静态红线（准音线）**绘制在 Canvas 上。
* **录音比对**：
  1. 用户点击界面“录制此段”，手机保持静音（无物理伴奏污染）。
  2. 采集用户清唱录音进行**动态绿线（用户线）**绘制。
  3. 录音结束时触发对比打分。
* **交互特性**：用户可在同一个片段反复进行 `[听 -> 停 -> 录 -> 比对]` 的循环死磕，满意后再推进时间轴。

---

## 3. 技术核心实现与数据流

### 模块三：Worker 多线程流式解码与分析 (Worker Processing)

#### 🛠️ FR-3.1 独立线程架构
* **处理逻辑**：在 `app.json` 中配置 `workers` 路径，核心数据处理（解码、切片、算法分析）严禁占用主线程，防止 UI 卡顿。

#### 🛠️ FR-3.2 异步预解码
* **处理逻辑**：用户选定音频后，主线程将轨道 B 的 `ArrayBuffer` 投递给 Worker。Worker 使用轻量解码器（WAV 原生解析 / MP3 Wasm 软解）将其一次性解析为全局 PCM 数组（`Float32Array`）常驻内存。

#### 🛠️ FR-3.3 音高提取与噪点容错
* **核心算法**：集成 `pitchfinder` 库中的 YIN 算法或自相关（ACF）算法模块至 Worker。
* **动态采样率自适应**：算法必须动态读取录音分片（`onFrameRecorded`）实际吐出的 `sampleRate`，严禁写死 $44,100\text{Hz}$，以兼容 iOS 端硬件（如蓝牙耳机）引起的采样率跳变。
* **静音门限控制**：音频能量（RMS）低于 $-40\text{dB}$ 时判定为无声区，音高置 0，防止环境噪音引入尖锐毛刺。

---

### 模块四：Canvas 2D 绘制、打分与权限层 (UI & Business Layer)

#### 🎨 FR-4.1 双轨 Canvas 2D 渲染
* **渲染技术**：使用小程序全新 Canvas 2D 接口 + `requestAnimationFrame`。
* **坐标轴定义**：
  * **X 轴**：当前切片相对时间。
  * **Y 轴**：MIDI 音高编号。
* **线条样式**：红线（原唱）居上，绿线（用户）实时在下方或重叠延展。

#### 🎨 FR-4.2 相似度打分
* **打分机制**：片段录音结束时，比对两组音高信号数组的重合度（建议采用 DTW 动态时间规整算法，允许用户录音有轻微的时间拉伸或错位错位），输出 $0 \sim 100$ 的匹配分。

#### 🎨 FR-4.3 平台环境隔离（iOS / Android 收费适配）

| 平台 (OS) | UI 展示 | 交互/付费引导逻辑 |
| :--- | :--- | :--- |
| **iOS 端** | 屏蔽任何“付费”、“充值”、“VIP”、“¥”等字眼。 | 将解锁按钮文案置换为**“激活高级功能”**。用户点击时弹出微信客服组件 `wx.openCustomerServiceChat` 或复制暗号弹窗，引导通过客服或外部网页激活。 |
| **Android 端** | 直接提供显式的购买/解锁按钮。 | 点击后调用 `wx.requestPayment` 发起微信支付。 |

#### 🎨 FR-4.4 功能权限栅栏 (Feature Gate)
* **鉴权逻辑**：本地缓存状态加密维护一个 `userStatus` 对象（含 `isVIP` 和 `dailyTriesLeft`）。在“跟读模式”下触发 `bindtouchend` 切片前进行拦截校验。若无权限，阻断 Worker 调用，弹出广告解锁或 VIP 激活弹窗。

---

## 4. 稳定性与健壮性保障 (Robustness)

### 🛡️ ER-5.1 内存红线强力自救
* **自救机制**：全局监听 `wx.onMemoryWarning()`。
* **触发逻辑**：一旦收到系统的内存警告通知（Level $\ge 2$）：
  1. 必须立即清空全局大数组缓存（对 PCM 数组执行 `fill(0)` 释放并置为 `null`）。
  2. 主动暂停当前录音与播放。
  3. 弹窗安全提示用户，绝对避免小程序闪退。

---

## 5. 项目骨架目录结构参考

```text
├── app.json                # 配置 window.navigationBarTitleText = "准音线" 及 workers 路径
├── app.js                  # 初始化全局权限状态 userStatus 
├── pages
│   └── index               # 主界面 (双轨导入、模式切换、Canvas绘制、手势捕获)
│       ├── index.wxml      # bindtouchstart/touchend 捕获跟读模式手势
│       ├── index.js        # 调用 InnerAudioContext 与 RecorderManager
│       └── index.wxss
└── workers                 # 多线程目录
    └── audioWorker         # 音频处理 Worker
        ├── index.js        # 接收 ArrayBuffer 进行流式切片与消息中转
        ├── decoder.js      # 轻量级音频解码器 (WAV/MP3)
        └── pitchfinder.js  # 纯数学音高提取算法 (YIN/ACF)
```

---

## 💡 给 Agent (Antigravity) 的开工启动 Prompt

你可以直接将本段话连同上方 PRD 一起发给 Antigravity：

> “请阅读上方小程序的 PRD。
> 
> **第一步**：请先帮我搭建整个项目的基本目录结构，并在 `app.json` 中配置好 `window` 标题为『准音线』以及 `workers` 多线程目录。
> 
> **第二步**：优先编写 `pages/index/index.js` 中用户选择文件后的 `saveFile` 本地转存逻辑（FR-1.3），以及跟读模式的 `bindtouchstart` / `bindtouchend` 时间戳捕获逻辑。
> 
> 完成后向我确认，我们再编写 Worker 内部的解码与算法。”